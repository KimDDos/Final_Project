<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.four.www.reservation.repository.ReservationDAO">



<insert id="register">
     insert into reservation (trainer_no,r_regdate,r_time,r_pw,r_content) 
     values(trainer_no=#{trainerNo},r_regdate=#{rvRegdate},r_time=#{rvTime},r_pw=#{rvPw},r_content=#{rvContent})
     </insert>
</mapper>